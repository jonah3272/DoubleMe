# Copy this file to .env.local and set values. Do not commit .env.local.
# All variables used by the app are listed below.

# ============ REQUIRED (app won't run without these) ============
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ============ OPTIONAL – App URL ============
# Required for Granola OAuth redirect. Vercel sets VERCEL_URL automatically.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============ OPTIONAL – Supabase service role ============
# Needed for: auth bypass (dev), seed-dev-user, any server-side admin operations.
# SUPABASE_SERVICE_ROLE_KEY=

# ============ OPTIONAL – Auth bypass (dev only) ============
# Skip login and act as this user. Requires SUPABASE_SERVICE_ROLE_KEY.
# AUTH_BYPASS=true
# BYPASS_USER_ID=
# BYPASS_EMAIL=

# ============ OPTIONAL – Granola MCP ============
# Import tasks/notes from Granola. Connect via Settings → Granola (OAuth) or set a bearer token.
# GRANOLA_MCP_URL=https://mcp.granola.ai/mcp
# GRANOLA_API_TOKEN=
# GRANOLA_DEBUG=1

# ============ OPTIONAL – Kimi (Moonshot) ============
# Synthesize meeting transcripts on Import from Granola. Get key: https://platform.moonshot.ai
# KIMI_API_KEY=

# ============ OPTIONAL – Google Calendar ============
# Create meetings from Settings → Calendar. OAuth client: https://console.cloud.google.com/apis/credentials
# Redirect URI: https://your-app/api/auth/google-calendar/callback
# GOOGLE_CALENDAR_CLIENT_ID=
# GOOGLE_CALENDAR_CLIENT_SECRET=
