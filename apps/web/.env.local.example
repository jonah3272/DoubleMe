# Copy to .env.local and set values. Do not commit .env.local.

# Supabase (from project settings)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only (if needed for service role or API)
# SUPABASE_SERVICE_ROLE_KEY=
# LLM_API_KEY=

# Auth bypass (dev only): skip login and act as this user. Requires SUPABASE_SERVICE_ROLE_KEY.
# Create the user in Supabase Auth (e.g. Jonahrehbeinjones@gmail.com), then set:
# AUTH_BYPASS=true
# BYPASS_USER_ID=<uuid-from-supabase-auth-user>
# BYPASS_EMAIL=Jonahrehbeinjones@gmail.com

# Dev password login: Jonahrehbeinjones@gmail.com / passwordstacy3272
# Run once to create or reset that user: curl -X POST http://localhost:3000/api/seed-dev-user
# (Requires SUPABASE_SERVICE_ROLE_KEY in .env.local)

# App URL (required for Granola OAuth "Connect to Granola" flow; e.g. https://your-app.vercel.app or http://localhost:3000)
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# Granola MCP (optional): import tasks/notes from Granola transcripts
# Connect via Settings → Granola MCP → "Connect to Granola" (OAuth, like Claude). Or set a bearer token:
# GRANOLA_MCP_URL=https://mcp.granola.ai/mcp
# GRANOLA_API_TOKEN=<bearer-token>

# Kimi (Moonshot) API: synthesize meeting transcripts into readable summaries on the Import from Granola page.
# Get key at https://platform.moonshot.ai
# KIMI_API_KEY=
# On Vercel: set KIMI_API_KEY in Project → Settings → Environment Variables, then redeploy.
