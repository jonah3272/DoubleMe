# Copy to .env and set values only if MCP tools need API or DB access.
# Do not commit .env.

# Optional: web app API base URL and key for MCP tools that call the app
# WEB_APP_URL=
# WEB_APP_API_KEY=

# Optional: Supabase (read-only or minimal write) if MCP talks to DB directly
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
