{
  "name": "doubleme",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "pnpm -F web dev",
    "start": "node run-web.js",
    "dev:web": "pnpm -F web dev",
    "dev:mcp": "pnpm -F mcp dev",
    "build": "pnpm -r run build",
    "build:web": "pnpm -F web build",
    "build:mcp": "pnpm -F mcp build",
    "lint": "pnpm -r run lint"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=20"
  }
}
