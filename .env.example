# Root-level example. Copy to apps/web/.env.local and apps/mcp/.env as needed.
# Do not commit files that contain real values.

# Web app (apps/web/.env.local)
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=

# MCP server (apps/mcp/.env) — only if tools need API or DB access
# WEB_APP_URL=
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
